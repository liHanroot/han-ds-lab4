server:
  port: 8070
spring:
  application.name: car-service
  profiles.active: prod
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
  mvc:
    format:
      date: dd/MM/yyyy
